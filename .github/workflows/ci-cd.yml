name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  DOTNET_VERSION: '8.0.x'
  NODE_VERSION: '20'
  AZURE_HOST: ${{ secrets.AZURE_HOST }}
  AZURE_USER: ${{ secrets.AZURE_USER }}
  RESOURCE_GROUP: 'EVEAbacus'
  VM_NAME: ${{ secrets.VM_NAME }}

jobs:
  # Build and test backend
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
      
      - name: Cache .NET dependencies
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-
      
      - name: Restore dependencies
        run: dotnet restore ./EVEAbacus.WebUI/EVEAbacus.WebUI.csproj
      
      - name: Build backend
        run: dotnet build --no-restore --configuration Release ./EVEAbacus.WebUI/EVEAbacus.WebUI.csproj
      
      - name: Test backend
        run: dotnet test --no-build --verbosity normal ./EVEAbacus.WebUI/EVEAbacus.WebUI.csproj
      
      - name: Publish backend
        run: dotnet publish --configuration Release --output ./publish-backend ./EVEAbacus.WebUI/EVEAbacus.WebUI.csproj
      
      - name: Upload backend artifacts
        uses: actions/upload-artifact@v4
        with:
          name: backend-artifacts
          path: ./publish-backend
          retention-days: 1

  # Deploy backend to production
  deploy-backend:
    needs: [build-backend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download backend artifacts
        uses: actions/download-artifact@v4
        with:
          name: backend-artifacts
          path: ./backend-artifacts
      
      - name: Debug downloaded artifacts
        run: |
          echo "🔍 Checking downloaded artifacts..."
          echo "Backend artifacts:"
          ls -la ./backend-artifacts/ || echo "Backend artifacts not found"
      
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: List available VMs
        run: |
          echo "Available VMs in resource group ${{ env.RESOURCE_GROUP }}:"
          az vm list --resource-group ${{ env.RESOURCE_GROUP }} --query "[].{Name:name, Status:powerState}" -o table
      
      - name: Deploy backend to Azure VM
        run: |
          # Stop backend service
          echo "Stopping backend service..."
          az vm run-command invoke \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.VM_NAME }} \
            --command-id RunShellScript \
            --scripts "
              sudo systemctl stop eveabacus 2>/dev/null || true
            "
          
          # Create storage account for large files
          echo "Setting up storage for backend deployment..."
          STORAGE_ACCOUNT_NAME="evebackend$(date +%s)"
          az storage account create \
            --name $STORAGE_ACCOUNT_NAME \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --location eastus \
            --sku Standard_LRS \
            --kind StorageV2
          
          # Get storage key
          STORAGE_KEY=$(az storage account keys list \
            --account-name $STORAGE_ACCOUNT_NAME \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --query '[0].value' -o tsv)
          
          # Create container
          az storage container create \
            --name deployments \
            --account-name $STORAGE_ACCOUNT_NAME \
            --account-key $STORAGE_KEY
          
          # Upload backend artifacts
          echo "Uploading backend artifacts..."
          tar -czf backend-artifacts.tar.gz -C ./backend-artifacts .
          az storage blob upload \
            --account-name $STORAGE_ACCOUNT_NAME \
            --account-key $STORAGE_KEY \
            --container-name deployments \
            --name backend-artifacts.tar.gz \
            --file backend-artifacts.tar.gz
          
          # Deploy backend from storage
          echo "Deploying backend from storage..."
          # Generate SAS token for backend artifacts
          BACKEND_SAS=$(az storage blob generate-sas \
            --account-name $STORAGE_ACCOUNT_NAME \
            --account-key $STORAGE_KEY \
            --container-name deployments \
            --name backend-artifacts.tar.gz \
            --permissions r \
            --expiry $(date -u -d "1 hour" '+%Y-%m-%dT%H:%M:%SZ') \
            --full-uri \
            --output tsv)
          
          az vm run-command invoke \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.VM_NAME }} \
            --command-id RunShellScript \
            --scripts "
              echo '🔍 Starting backend deployment...'
              sudo mkdir -p /var/www/eveabacus
              sudo rm -rf /var/www/eveabacus/*
              cd /tmp
              echo '📥 Downloading backend artifacts...'
              curl -L -o backend-artifacts.tar.gz \"$BACKEND_SAS\"
              echo '📦 Extracting backend artifacts...'
              tar -xzf backend-artifacts.tar.gz -C /var/www/eveabacus/
              echo '🔧 Setting permissions...'
              sudo chown -R eveabacus:eveabacus /var/www/eveabacus
              sudo chmod -R 755 /var/www/eveabacus
              echo '📁 Backend deployment contents:'
              ls -la /var/www/eveabacus/
              rm -f backend-artifacts.tar.gz
              echo '✅ Backend deployment complete'
            "
          
          # Clean up storage account
          echo "Cleaning up storage account..."
          az storage account delete \
            --name $STORAGE_ACCOUNT_NAME \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --yes
          
          # Start backend service
          echo "Starting backend service..."
          az vm run-command invoke \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.VM_NAME }} \
            --command-id RunShellScript \
            --scripts "
              echo '🔧 Starting backend service...'
              # Start backend service if it exists
              if systemctl list-unit-files | grep -q eveabacus.service; then
                echo '🚀 Starting eveabacus service...'
                sudo systemctl start eveabacus
                echo '⏳ Waiting for backend to be ready...'
                sleep 10
                echo '✅ Backend service started'
              else
                echo '⚠️  eveabacus service not found'
              fi
            "
          
          # Wait for backend to be ready
          echo "Waiting for backend to be ready..."
          az vm run-command invoke \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.VM_NAME }} \
            --command-id RunShellScript \
            --scripts "
              echo '🔍 Checking if backend is ready...'
              for i in {1..30}; do
                if curl -f http://localhost:5000/health 2>/dev/null; then
                  echo '✅ Backend is ready!'
                  exit 0
                fi
                echo '⏳ Waiting for backend to be ready... (attempt $i/30)'
                sleep 5
              done
              echo '⚠️  Backend may not be fully ready, but continuing...'
            "

  # Build frontend (after backend is deployed and running)
  build-frontend:
    needs: [deploy-backend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest
      
      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      
      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      
      - name: Setup Next.js cache
        uses: actions/cache@v4
        with:
          path: |
            ./eve-abacus-webui/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('**/*.ts', '**/*.tsx', '**/*.js', '**/*.jsx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-
            ${{ runner.os }}-nextjs-
      
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: ./eve-abacus-webui
      
      - name: Build frontend
        run: pnpm build
        working-directory: ./eve-abacus-webui
      
      - name: Check if build artifacts exist
        run: |
          if [ ! -d "./eve-abacus-webui/.next" ]; then
            echo "❌ Frontend build failed - .next directory not found"
            exit 1
          fi
          echo "✅ Frontend build artifacts found"
          ls -la ./eve-abacus-webui/.next/
          
          # Check for standalone output
          if [ -d "./eve-abacus-webui/.next/standalone" ]; then
            echo "✅ Standalone output found"
            ls -la ./eve-abacus-webui/.next/standalone/
            echo "📁 Checking for server.js in standalone:"
            if [ -f "./eve-abacus-webui/.next/standalone/server.js" ]; then
              echo "✅ server.js found in standalone build"
            else
              echo "⚠️  server.js not found in standalone build"
              echo "📁 Available files in standalone:"
              find ./eve-abacus-webui/.next/standalone -type f -name "*.js" | head -10
            fi
          else
            echo "⚠️  Standalone output not found, using regular .next directory"
            echo "📁 Available files in .next:"
            find ./eve-abacus-webui/.next -type f -name "*.js" | head -10
          fi
        working-directory: ./
      
      - name: Debug frontend artifacts
        run: |
          echo "📁 Frontend artifacts to be uploaded:"
          echo "Contents of .next directory:"
          find ./eve-abacus-webui/.next -type f -name "*.js" | head -10
          echo ""
          echo "Contents of public directory:"
          ls -la ./eve-abacus-webui/public/
        working-directory: ./
      
      - name: Prepare frontend artifacts
        run: |
          mkdir -p ./frontend-build
          if [ -d "./eve-abacus-webui/.next/standalone" ]; then
            echo "🚀 Using standalone build..."
            cp -r ./eve-abacus-webui/.next/standalone/* ./frontend-build/
          else
            echo "📁 Using regular build..."
            cp -r ./eve-abacus-webui/.next ./frontend-build/
            cp -r ./eve-abacus-webui/public ./frontend-build/
          fi
          # Always include package.json for pnpm start to work
          cp ./eve-abacus-webui/package.json ./frontend-build/
          echo "📁 Final frontend artifacts:"
          ls -la ./frontend-build/
        working-directory: ./
      
      - name: Upload frontend artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-artifacts
          path: ./frontend-build
          retention-days: 1

  # Deploy frontend to production
  deploy-frontend:
    needs: [build-frontend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download frontend artifacts
        uses: actions/download-artifact@v4
        with:
          name: frontend-artifacts
          path: ./frontend-artifacts
      
      - name: Debug downloaded artifacts
        run: |
          echo "🔍 Checking downloaded artifacts..."
          echo "Frontend artifacts:"
          ls -la ./frontend-artifacts/ || echo "Frontend artifacts not found"
          echo ""
          echo "Frontend .next directory:"
          ls -la ./frontend-artifacts/.next/ || echo ".next directory not found"
          echo ""
          echo "Frontend public directory:"
          ls -la ./frontend-artifacts/public/ || echo "public directory not found"
      
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: Deploy frontend to Azure VM
        run: |
          # Stop frontend service
          echo "Stopping frontend service..."
          az vm run-command invoke \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.VM_NAME }} \
            --command-id RunShellScript \
            --scripts "
              sudo systemctl stop eveabacusfrontend 2>/dev/null || true
            "
          
          # Create storage account for large files
          echo "Setting up storage for frontend deployment..."
          STORAGE_ACCOUNT_NAME="evefrontend$(date +%s)"
          az storage account create \
            --name $STORAGE_ACCOUNT_NAME \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --location eastus \
            --sku Standard_LRS \
            --kind StorageV2
          
          # Get storage key
          STORAGE_KEY=$(az storage account keys list \
            --account-name $STORAGE_ACCOUNT_NAME \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --query '[0].value' -o tsv)
          
          # Create container
          az storage container create \
            --name deployments \
            --account-name $STORAGE_ACCOUNT_NAME \
            --account-key $STORAGE_KEY
          
          # Upload frontend artifacts
          echo "Uploading frontend artifacts..."
          tar -czf frontend-artifacts.tar.gz -C ./frontend-artifacts .
          az storage blob upload \
            --account-name $STORAGE_ACCOUNT_NAME \
            --account-key $STORAGE_KEY \
            --container-name deployments \
            --name frontend-artifacts.tar.gz \
            --file frontend-artifacts.tar.gz
          
          # Deploy frontend from storage
          echo "Deploying frontend from storage..."
          # Generate SAS token for frontend artifacts
          FRONTEND_SAS=$(az storage blob generate-sas \
            --account-name $STORAGE_ACCOUNT_NAME \
            --account-key $STORAGE_KEY \
            --container-name deployments \
            --name frontend-artifacts.tar.gz \
            --permissions r \
            --expiry $(date -u -d "1 hour" '+%Y-%m-%dT%H:%M:%SZ') \
            --full-uri \
            --output tsv)
          
          az vm run-command invoke \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.VM_NAME }} \
            --command-id RunShellScript \
            --scripts "
              echo '🔍 Starting frontend deployment...'
              sudo mkdir -p /var/www/eveabacus/frontend
              sudo rm -rf /var/www/eveabacus/frontend/*
              cd /tmp
              echo '📥 Downloading frontend artifacts...'
              curl -L -o frontend-artifacts.tar.gz \"$FRONTEND_SAS\"
              echo '📦 Extracting frontend artifacts...'
              tar -xzf frontend-artifacts.tar.gz
              echo '📁 Extracted frontend contents:'
              ls -la /tmp/
              echo '📁 .next directory contents:'
              ls -la /tmp/.next/ || echo '.next directory not found'
              echo '📁 public directory contents:'
              ls -la /tmp/public/ || echo 'public directory not found'
              if [ -d '/tmp/.next/standalone' ]; then
                echo '🚀 Using standalone build...'
                sudo cp -r /tmp/.next/standalone/* /var/www/eveabacus/frontend/
                echo '📁 Standalone contents copied:'
                ls -la /var/www/eveabacus/frontend/
              else
                echo '📁 Using regular build...'
                sudo cp -r /tmp/.next /var/www/eveabacus/frontend/
                sudo cp -r /tmp/public /var/www/eveabacus/frontend/
                echo '📁 Regular build contents copied:'
                ls -la /var/www/eveabacus/frontend/
              fi
              echo '🔧 Setting permissions...'
              sudo chown -R www-data:www-data /var/www/eveabacus/frontend
              sudo chmod -R 755 /var/www/eveabacus/frontend
              echo '📁 Final frontend deployment contents:'
              ls -la /var/www/eveabacus/frontend/
              if [ -f '/var/www/eveabacus/frontend/server.js' ]; then
                echo '✅ server.js found - standalone build successful'
              else
                echo '⚠️  server.js not found - checking for alternative files'
                find /var/www/eveabacus/frontend -name '*.js' | head -5
              fi
              rm -f frontend-artifacts.tar.gz
              echo '✅ Frontend deployment complete'
            "
          
          # Clean up storage account
          echo "Cleaning up storage account..."
          az storage account delete \
            --name $STORAGE_ACCOUNT_NAME \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --yes
          
          # Start frontend service and reload nginx
          echo "Starting frontend service and reloading nginx..."
          az vm run-command invoke \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.VM_NAME }} \
            --command-id RunShellScript \
            --scripts "
              echo '🔧 Starting frontend service and reloading nginx...'
              
              # Update the service configuration to use standalone build
              echo '🔧 Updating frontend service configuration...'
              sudo systemctl stop eveabacusfrontend 2>/dev/null || true
              sudo rm -f /etc/systemd/system/eveabacusfrontend.service
              echo '[Unit]' | sudo tee /etc/systemd/system/eveabacusfrontend.service
              echo 'Description=EVE Abacus Next.js Frontend' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'After=network.target' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo '' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo '[Service]' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'WorkingDirectory=/var/www/eveabacus/frontend' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'ExecStart=/usr/bin/node /var/www/eveabacus/frontend/server.js' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'Restart=always' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'RestartSec=10' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'SyslogIdentifier=eveabacusfrontend' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'User=www-data' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'Environment=NODE_ENV=production' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'Environment=PORT=3000' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo '' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo '[Install]' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              echo 'WantedBy=multi-user.target' | sudo tee -a /etc/systemd/system/eveabacusfrontend.service
              
              # Reload systemd to pick up the new configuration
              sudo systemctl daemon-reload
              
              # Start frontend service if it exists
              if systemctl list-unit-files | grep -q eveabacusfrontend.service; then
                echo '🚀 Starting eveabacusfrontend service...'
                sudo systemctl start eveabacusfrontend
                echo '⏳ Waiting for frontend service to start...'
                sleep 5
                if systemctl is-active --quiet eveabacusfrontend; then
                  echo '✅ Frontend service started successfully'
                else
                  echo '⚠️  Frontend service failed to start'
                  sudo systemctl status eveabacusfrontend
                fi
              else
                echo '⚠️  eveabacusfrontend service not found'
              fi
              
              # Reload nginx if it exists
              if systemctl list-unit-files | grep -q nginx.service; then
                echo '🔄 Reloading nginx...'
                sudo systemctl reload nginx
              else
                echo '⚠️  nginx service not found'
              fi
              
              echo '✅ Frontend deployment and service startup complete'
            " 